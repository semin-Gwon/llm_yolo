# Wired Dev Mode

This project assumes a wired LAN link between the robot and the development PC.

Implications:
- heavy compute stays offboard
- ROS 2 traffic stays on the PC first during MVP bring-up
- the robot still needs a minimal runtime and fail-safe layer
